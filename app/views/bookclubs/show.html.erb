<div class="container mt-5">
  <div class="p-4 bg-light rounded shadow-sm">
    <h2 style="color: #111827;"><%= @bookclub.name %> Members</h2>

    <% if @users.any? %>
      <ul class="list-group mt-13 flex gap-2">
        <% @users.each do |user| %>
          <li class="list-group-item">
            👤 <strong><%= user.name %></strong>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-muted mt-3">No users have joined this book club yet.</p>
    <% end %>

    <%= link_to "Back", action_bookclubs_path(do: "index"), class: "btn btn-secondary mt-4" %>
  </div>
</div>
